defaults: &defaults

development:
  <<: *defaults
  domain: localhost
  port: 3000

test:
  <<: *defaults
  domain: test.host
  port: 80

production:
  <<: *defaults
  domain: theitskillboard.com
  port: 80