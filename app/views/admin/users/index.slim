h1 Users

table border="1" width="100%"
  thead
    tr
      th Id
      th Name
      th Surname
      th Email
      th Created at
      th Updated at
  tbody
    - @users.each do |user|
      tr
        td #{user.id}
        td #{user.name || "n/a"}
        td #{user.surname || "n/a"}
        td #{user.email}
        td #{user.created_at}
        td #{user.updated_at}
